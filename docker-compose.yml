version: '3.9'
services:
    mariadb:
        image: mariadb:10.7.3
        restart: always
        ports:
            - 3306:3306
        environment:
            - MYSQL_ROOT_PASSWORD=foobar
        volumes:
            - ./mysql:/var/lib/mysql
    phpmyadmin:
        image: phpmyadmin:5.1.3-apache
        restart: always
        ports:
            - 1337:80
        environment:
            - PMA_HOST=mariadb
            - PMA_USER=root
            - PMA_PASSWORD=foobar
        depends_on:
            - mariadb
